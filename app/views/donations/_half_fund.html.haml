- if current_user && current_user.organization? && !news_item.half_funded?
  .half_fund
    = link_to image_tag('half_fund.gif'), half_fund_pitch_path(news_item), :method => :put, :title => "Half Fund"
